name: CI
on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/amio-dev

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pre-commit/action@v3.0.1

  build-and-test:
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        build_type: [Debug, Release]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build dev container (cached)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: .devcontainer/Dockerfile
          load: true
          tags: amio-dev:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build and Test
        run: |
          docker run --rm -v ${{ github.workspace }}:/src:ro -w /tmp/build amio-dev:latest \
            bash -c "cmake /src -GNinja \
              -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
              -DAMIO_BUILD_TESTING=ON \
              -DAMIO_BUILD_EXAMPLES=ON \
              -DAMIO_FORCE_NCZARR=ON \
            && ninja -j\$(nproc) \
            && ctest --output-on-failure -L unit --timeout 120 \
            && RC_PARAMS='max_success=20' ctest --output-on-failure -L pbt --timeout 300"

  # Build the Docker image and push to GHCR (only on develop)
  docker:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: .devcontainer/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/amio-dev:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
